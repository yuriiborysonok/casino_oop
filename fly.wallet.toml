app = 'casino-wallet'
primary_region = 'fra'

[build]
  dockerfile = "Dockerfile.wallet"

[http_service]
  internal_port = 8081
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0

[[vm]]
  memory = '512mb'
  cpu_kind = 'shared'
  cpus = 1
